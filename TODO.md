# TODO

- [x] Tambahkan route logout bernama `logout` (POST /logout) ke `routes/web.php` mengarah ke `AuthController@logout` dengan middleware `auth`.
- [ ] Verifikasi bahwa navbar logout form (`route('logout')`) tidak lagi error.
- [ ] Jalankan `php artisan route:list` untuk memastikan route `logout` terdaftar.


